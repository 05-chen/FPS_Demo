# 项目框架说明（Steam 1v1 FPS）

**技术栈：** Unity 2022+ · Netcode for GameObjects (NGO) 1.8+ / 1.11 · Steamworks.NET · SteamNetworkingSockets P2P

**测试 AppID：** `480`（Spacewar）

**规范参考：** 根目录 `.cursorrules` / `AGENTS.md`（禁止 UNet，强制 NGO 写法与命名规范）

**配套文档：**


| 文档                  | 用途                                                               |
| ------------------- | ---------------------------------------------------------------- |
| `Player预制体结构说明书.md` | `Player.prefab` 层级、组件、Hitbox、枪/ADS 节点与已知缺口 |
| `开枪音效排查笔记.md`       | 有弹孔没声音、导入器静音、Mute Audio 等音频排查                                    |
| `接下来三天的开发日程.md`     | 单机底层完善计划（枪模挂载、跳跃动画、蓝方 Avatar 等）                                 |


当前阶段：联机大厅 + 延迟出生 + 倒地/死亡/自动复活已通。单机用 UnityTransport Host 留在练习场景；联机用 Steam P2P 切到对局场景。

玩家角色模型已接入 `Assets/Model/Soldier/redSolider.fbx` / `blueSolider.fbx`（Mixamo 人形骨架 + 内嵌贴图）。装配 Player 走两个 Editor 菜单：**Tools/Build Player in Hierarchy**（从零拼一套）与 **Tools/Attach Components To Selected Player**（给选中的 Player 补挂 / 修正）。枪械与 ADS 对齐另用 **Tools/Setup UpperBody Weapon and ADS**（补 `WeaponSocket` / 枪模 / `SightPoint` / `CameraSightTarget`，清洗 Layer&Collider，接线 `WeaponADS`）。动画相关：**Auto Bind Player Animator** / **Check Player Animator Setup** / **Fix ADS Fire Transitions** / **Validate and Fix Animation Settings**。

**当前主 PlayerPrefab = `Assets/Player/Player.prefab`**（`Testcene` + `GameList.asset` 已指向它）。`Player 1.prefab` 已不存在。另有 `Assets/Player/Player Test.prefab`：`Testcene_GamePlay` 的 NetworkManager 仍指向它——**联机对局场景尚未统一到 `Player.prefab`，合并前不要删**。

枪模与 ADS：主预制体已挂 **M1 Garand Lowpoly**（Layer = Ignore Raycast）；`WeaponSocket` 下有枪模 + `SightPoint`；`PlayerCamera` 下有 `CameraSightTarget`。`WeaponADS` 机瞄时把 `SightPoint` 对齐到 `CameraSightTarget`（屏幕中心），腰射仍用 `hipfirePosition` 本地偏移。旧的手臂诊断 / Character Fixer / Attach·Refit·Flip 枪模工具已移除。

射击判定已修掉「CharacterController 挡住骨骼 Hitbox」：`PickBestAlongRay` 穿透 CC、Hitscan / 实弹共用「最佳部位」结论、假人反馈加 `PracticeDummyHealth` 门闸，细节见第 6、7 节。

角色动画已接入：`PlayerAnimatorController` + `PlayerAnimationManager`（摄像机 LateUpdate 仅跟随 Head 位置）。**跳跃状态尚未建**（只有 `Jump` 参数）。`redSolider.fbx` 为 Humanoid；**`blueSolider.fbx` 仍是 Generic**，蓝方不会播动画。

站立高度三处保持一致：`CharacterController.height = 1.6`、`ModelRoot.localPosition.y ≈ -0.8`、`PlayerController.standingHeight = 1.6`。

战区：线性 5 区严格邻接 + 五格顶栏 + `MatchGameManager` 推平/限时结算。尚未做：圈外网格加权、Tickets、倒地队友救援、跳跃状态机、蓝方 Avatar、联机场景 PlayerPrefab 统一、`gunSound` 绑定、机瞄姿态 Scene 微调。

---



## 1. 目录结构

```
Assets/
├── Scripts/
│   ├── Core/                 # 全局基础设施：门闩、阵营枚举、伤情枚举、玩家名单
│   ├── Player/               # 角色：移动、视角、生命状态机、伤情 Debuff、动画驱动、Owner 权威变换
│   ├── Weapon/               # 射线 / 实体弹 / 弹孔 / ADS / 后坐力 / 抛壳
│   ├── Enemy/                # 练习假人、部位 Hitbox（不放共用枚举）
│   ├── World/                # 线性 5 战区：网格 / 核心圈 / 邻接解锁 / 占领 / 局内结算
│   ├── Managers/             # SpawnManager（选阵营后出生、阵营复活点）
│   ├── SteamConnect/         # Steam 大厅、短邀请码、P2P 传输
│   ├── UI/                   # 选阵营、倒地/死亡 HUD、五格战区顶栏、结算 UI
│   ├── UIinteraction/        # 大厅工厂、暂停、旧 FactionSelect 转发器
│   └── Steamworks.NET/       # Steam 官方启动器（尽量少改）
├── Tests/
│   ├── Editor/               # 白盒 NUnit（SectorLogicTests）+ Inspector 测试按钮
│   └── Runtime/              # 灰盒 Harness / 黑盒物理 Trigger / Dummy / 报告
├── Player/
│   ├── Player.prefab         # 当前主 PlayerPrefab（Testcene + GameList）
│   ├── Player Test.prefab    # 实验副本；Testcene_GamePlay 的 PlayerPrefab 仍指向它
│   ├── Bullet.prefab         # 实体弹（NetworkObject + NetworkTransform + Projectile）
│   ├── Shell.prefab          # 抛壳
│   ├── Quad.prefab           # 弹孔占位
│   └── TestEnemy/PracticeDummy.prefab
├── Model/
│   ├── Soldier/
│   │   ├── redSolider.fbx    # 红方（Humanoid + 主 Avatar）
│   │   ├── blueSolider.fbx   # 蓝方（⚠ 仍是 Generic / 无 Avatar）
│   │   └── Animations/       # 11 个动画 FBX
│   └── Gun/
│       └── M1 Garand Lowpoly.fbx  # 枪模资产；主 Player.prefab 已实例化挂载
├── Animations/
│   └── PlayerAnimatorController.controller
├── UpperBodyMask.mask
├── Scenes/
│   ├── Testcene.unity              # 启动 / 大厅 / 单机练习（Build 0）；PlayerPrefab → Player.prefab
│   ├── Testcene_GamePlay.unity     # 联机对局（Build 1）；⚠ PlayerPrefab → Player Test.prefab
│   └── ModelScene.unity            # 搭模型 / 跑 Tools 菜单（不进 Build）
├── Source/Audio/gun.wav
├── Editor/
│   ├── BuildPlayerHierarchy.cs      # Tools/Build Player in Hierarchy
│   ├── AttachComponentsToPlayer.cs  # Attach Components + Setup UpperBody Weapon and ADS
│   ├── PlayerHierarchyUtils.cs      # 装配共用：节点 / Layer / Collider / 引用写入
│   ├── PlayerAnimatorBinder.cs      # Tools/Auto Bind Player Animator
│   ├── AnimatorSetupChecker.cs      # Tools/Check Player Animator Setup
│   ├── AnimatorAdsFireFixer.cs      # Tools/Fix ADS Fire Transitions
│   ├── AnimationSettingsValidator.cs # Tools/Validate and Fix Animation Settings
│   ├── RefreshNetworkPrefabIds.cs
│   └── SteamBuildCopyAppId.cs
├── DefaultNetworkPrefabs.asset      # 含 Player.prefab + Player Test.prefab
└── GameList.asset                   # 正式列表：Player.prefab + Bullet.prefab
```


| 层                      | 职责                                                | 原则                                                 |
| ---------------------- | ------------------------------------------------- | -------------------------------------------------- |
| **Core**               | 日志、门闩、输入快照、`TeamId` / `InjuryState`、玩家名单、场景名、调试开关 | 不依赖 UI / Steam / 假人                                |
| **Player**             | 本地操作、姿态、生命状态机、伤情 Debuff、动画驱动                    | 联网必须 `IsOwner` 隔离                                  |
| **Weapon**             | 开火、弹道、弹孔、ADS、后坐力                                  | 伤害在服务器；VFX 用 ClientRpc 本地生成                        |
| **Enemy**              | 假人部位、伤情映射、镜面反弹                                    | 只服务练习；共用枚举在 Core                                   |
| **World**              | 5 战区网格 + 核心圈、邻接解锁、占领进度、局内倒计时与胜负                   | 仅服务器算进度 / 结算；客户端只读 NV；规则纯函数可单测                     |
| **Managers**           | 选完阵营才 `SpawnAsPlayerObject`，按阵营区域复活               | 仅服务器调用 `SpawnForClient(TeamId, clientId)`          |
| **SteamConnect**       | 开房、加入、切场景、记住阵营                                    | 状态机驱动；对局中禁止把 Host 拉回大厅 UI                          |
| **UI / UIinteraction** | 界面与流程跳转                                           | 只订阅 Session / Gate / Spawn / Sector / Match，不写玩法逻辑 |


---



## 2. 一局游戏流程

```
启动 Testcene
  → SteamManager 初始化 Steam
  → SteamLobbyUI 大厅（GameplayGate.FullBlock，俯视相机）
        ├─ 创建房间 → 6 位邀请码 → SteamTransport StartHost
        ├─ 输入邀请码加入 → 查大厅 → StartClient
        └─ 单机练习 → UnityTransport StartHost（不走 Steam）
  → ConnectionApproval：批准进房，但 CreatePlayerObject = false（先不生成玩家）
  → 双方到齐（或单机立刻）→ FactionSelectUI 选红 / 蓝
  → SteamLobbySession
        ├─ 单机：场景已是 Testcene，跳过二次 LoadScene，SpawnManager.SpawnForClient
        └─ 联机：LoadScene(Testcene_GamePlay) → 再 SpawnForClient
  → Owner 端 TeleportToSpawn + GameplayGate.Release → 锁鼠标，可操作
  → 1 = Hitscan，2 = Projectile；右键机瞄；左键开火
  → HP 打到 0 → 倒地（可长按空格放弃）→ 彻底死亡黑屏 5 秒
        → 服务器按已存 TeamId 自动 SpawnForClient，不再弹选阵营
        → 黑屏必须等 NotifyPlayerSpawnedClientRpc 才关（倒计时到 0 仍显示「等待服务器确认重生...」）
  → 走进核心圈 Sphere Trigger：仅 Alive 且「可进圈」时计入战力并拉动 CaptureProgress
        （可攻=邻接己方领地；己方已占领区含 HQ 永远可防守进人，与进攻解锁解耦）
  → 顶栏 SectorHUDUI：正中倒计时 + A~E 五格（锁头 / 灰底或红蓝拼接 / 徽章）
  → 推平（红占 E / 蓝占 A）或倒计时结束 → MatchEndUI → 延迟回大厅
  → Esc → PauseMenu（继续 / 退出到大厅）
```



### 场景约定（`GameScenes.cs`）


| 常量                | 场景                  | 何时用        |
| ----------------- | ------------------- | ---------- |
| `OfflinePractice` | `Testcene`          | 启动、大厅、单机练习 |
| `OnlineMatch`     | `Testcene_GamePlay` | Steam 联机对局 |


两个场景都必须勾进 **File → Build Settings**。`EnableSceneManagement` 必须打开。切场景时 `SceneHubGuard` 会销毁重复的 NetworkManager / SteamLobby（旧的那份已经 DontDestroyOnLoad）。

### 延迟出生（很重要）

进房时**不**自动生成 PlayerPrefab。选完阵营后，服务器 `SpawnAsPlayerObject`。这样不会出现「人已经在场，UI 还在选阵营」或导出包切场景失败。

玩家移动是 **Owner 权威**（`ClientNetworkTransform`）。服务器直接改 `transform.position` 会被 Owner 下一帧盖掉。复活传送必须走 Owner 的 `TeleportToSpawn` → `NetworkTransform.Teleport`。

---



## 3. Core（基础设施）


| 文件                      | 作用                                                                                      |
| ----------------------- | --------------------------------------------------------------------------------------- |
| `TeamId.cs`             | 阵营枚举 + `TeamIdUtil`：`None=0` / `Red=1` / `Blue=2`。**禁止**再用 0=红 / 1=蓝 的数组下标              |
| `InjuryState.cs`        | 玩家与假人共用伤情：`None` / `Light_Arms` / `Crippled_Legs` / `DBNO_Torso` / `InstanceDeath_Head` |
| `GameLog.cs`            | 带分类前缀的统一日志                                                                              |
| `GameplayGate.cs`       | 输入门闩，见下表                                                                                |
| `PauseGate.cs`          | Esc 暂停；联机不把 `Time.timeScale` 设为 0                                                       |
| `GameplayInputState.cs` | 一帧输入快照                                                                                  |
| `PlayerRegistry.cs`     | 登记在场玩家；查本地 Owner、阵营占用、`GetPlayerTeam(clientId)`                                         |
| `SteamRuntime.cs`       | Steam 是否就绪 / 昵称                                                                         |
| `GameScenes.cs`         | 单机 / 联机场景名                                                                              |
| `SceneHubGuard.cs`      | 切场景时干掉重复的网络中枢                                                                           |
| `DebugCommandGate.cs`   | **Alt+F9** 玩家伤情测试模式总开关（与战区 F9 热键分开）                                                     |




### GameplayGate 三态


| 模式            | 何时         | 操作     | 玩家相机       | 鼠标   |
| ------------- | ---------- | ------ | ---------- | ---- |
| `Open`        | 局内正常       | 允许     | 开          | 锁定   |
| `InputLocked` | 倒地等待救援     | 锁移动/开火 | **保持开启**   | 保持锁定 |
| `FullBlock`   | 大厅 / 首次选阵营 | 全锁     | 关掉（改用俯视相机） | 释放   |


倒地**不要**调用 `Block()`，要调用 `BlockInputOnly()`，否则相机会被关掉，倒地视角看不见。

### 输入一览


| 操作    | 按键                             | 说明                                                    |
| ----- | ------------------------------ | ----------------------------------------------------- |
| 移动    | WASD                           | 倒地 / 死亡时为 0                                           |
| 视角    | 鼠标                             | 需鼠标锁定                                                 |
| 跳跃    | Space                          | 倒地时禁用；倒地时长按空格 = 放弃救援                                  |
| 下蹲    | **按住 LeftControl**             | 腿伤时强制下蹲                                               |
| 奔跑    | **点一下 LeftShift** 锁住，停 WASD 解除 | 下蹲 / 腿伤时不能跑                                           |
| 开火    | 鼠标锁定后左键 `Fire1`                | 枪声只在左键按下时播                                            |
| 机瞄    | 右键                             | `WeaponADS`：`SightPoint`→`CameraSightTarget`；不进 `FirePressed` |
| 重新锁鼠标 | 未锁定时点左键                        | 只锁鼠标，不开火                                              |
| 切枪    | 数字键 1 / 2                      | Hitscan / Projectile                                  |
| 暂停    | Esc                            | `PauseGate`                                           |
| 测试模式  | **Alt+F9**                     | 默认关闭；ON 后玩家 F1–F5 伤情调试才生效（勿与战区 F9 混淆）                 |
| 伤情调试  | F1–F5                          | 见第 4 节，必须先 Alt+F9                                     |
| 战区测试  | 小键盘 1–5 / **F5–F12**           | 见第 8.6 节；**不需要**先开伤情测试。测战区时不要开 Alt+F9，否则 F5 会同时触发玩家爆头 |


开火必须 `Cursor.lockState == Locked` 才算 `FirePressed`。换新输入系统时，优先只改 `GameplayInputState.Read()`。

> 默认 Input Manager 里 `Fire1` 还绑了 Left Ctrl。下蹲也是 LeftControl，不要用 `GetButton("Fire1")` 当「按住开火」。

---



## 4. Player（角色）


| 文件                                              | 作用                                                              |
| ----------------------------------------------- | --------------------------------------------------------------- |
| `PlayerController.cs`                           | 编排器：输入 → 视角 / 姿态 / 移动 / 武器；阵营记忆；贴地与虚空回收                         |
| `CharacterMotor.cs`                             | `CharacterController` 移动 + 重力                                   |
| `FirstPersonLook.cs`                            | 身体左右转，摄像机俯仰；可叠加 `ProceduralRecoil.CameraRecoilOffset`          |
| `ClientNetworkTransform.cs`                     | **拥有者权威**位置同步；`TeleportToSpawn`                                 |
| `PlayerHealth.cs`                               | 生命 + `PlayerLifeState` 状态机 + 保存 `TeamId`（NetworkVariable 存枚举整型） |
| `PlayerStatusController.cs`（`namespace Player`） | 当前 `InjuryState`；F1–F5；倒地长按空格放弃                                 |
| `PlayerAnimationManager.cs`                     | 驱动 Animator 参数；`LateUpdate` 仅位置跟随 Head 骨骼；强制关 `applyRootMotion` |




### 联网铁律

`PlayerController.Update` 中：

1. 已死亡 → 直接 return（不处理输入）
2. `IsSpawned && !IsOwner` → return
3. `GameplayGate.IsBlocked` 或暂停 → 不处理操作；`InputLocked` 时仍插值倒地姿态 / 重力
4. 单机未 Spawn 时看 `IsControlled`



### 生命周期状态机（`PlayerLifeState`）

真正的「死没死」看 `PlayerHealth.LifeState`，不要只用 `HP == 0`。倒地时 HP 也是 0，但人还没彻底死。

```
Alive  ──HP 打到 0──►  Downed  ──15s 失血 / 长按空格 1.5s──►  Dead
                                              │
爆头 instantKill ─────────────────────────────┘
                                              │
                         5 秒黑屏后自动复活 ──► Alive + 传送回阵营点
```


| 状态         | 画面                                                           | 控制            | 接下来                                            |
| ---------- | ------------------------------------------------------------ | ------------- | ---------------------------------------------- |
| **Alive**  | 正常第一人称                                                       | 正常            | 受伤                                             |
| **Downed** | 不黑屏，贴地视角；「正在等待救援中...」+ 失血倒计时；长按空格进度条                         | `InputLocked` | 放弃或失血结束 → Dead                                 |
| **Dead**   | 全屏黑屏 Fade；`YOU ARE DEAD / 你已阵亡`；倒计时结束后改为「等待服务器确认重生...」直到 RPC | 无操作           | 服务器按已存 `TeamId` 调用 `SpawnForClient`，**不再弹选阵营** |


阵营**全程**用 `TeamId`（`None=0, Red=1, Blue=2`）。网络上若必须传 `int`，读写都走 `TeamIdUtil.FromNetwork`，非法值当成 `None`。`PlayerController.PersistTeam` 与 `PlayerHealth.RememberSpawnTeam` 都存枚举整型，不再做 0/1 数组映射。

`InjuryState` 在 `Core`。`PlayerHealth` 把 Downed/Dead 映射成 `DBNO_Torso` / `InstanceDeath_Head` 给姿态脚本，**不要** `using Enemy`。

`LifeState` 回到 Alive **不会**关掉死亡黑屏；关 UI 与 `GameplayGate.Release` 由 `SpawnManager.NotifyPlayerSpawnedClientRpc` 触发，避免延迟下提前亮屏。SpawnManager 失败时才走 Owner 本地 fallback。

死亡时禁用 Controller / Renderer / Collider，但**不**关 `NetworkTransform`。复活由 Owner `TeleportToSpawn` 设坐标。

### 姿态与胶囊（重要）

玩家预制体 `lossyScale` 约为 **0.5**。`transform` 在**胶囊中心**，不是脚底。

改高度时：

```
center.y = (height - standingHeight) * 0.5f
```

底部始终停在 `-standingHeight/2`。**不要**写成 `center.y = height/2`，否则碰撞体会浮空。倒地时高度会低于 `2 * radius`，所以半径会随高度缩小。

摄像机本地 Y：脚相对高度减去 `standingHeight/2`。默认：站立头高 1.6、下蹲 0.8、倒地 0.2。


| 状态          | 移速              | 胶囊 / 相机  |
| ----------- | --------------- | -------- |
| 走           | 4               | 站立       |
| 跑           | 7               | 站立       |
| 蹲 / 腿伤      | 2               | 下蹲高度     |
| 轻伤手臂        | 当前速度 × 0.8（仍可跑） | 站立或蹲     |
| 躯干倒地 / 爆头死亡 | 不能 WASD / 跳 / 跑 | 贴地高度 0.4 |




### 调试键（必须先按 Alt+F9）

控制台会打印 `Debug Mode Toggle: ON/OFF`。默认 OFF，防止误触把局打乱。单独按 **F9** 是战区临界进度热键，不会开伤情测试。


| 键      | 作用              |
| ------ | --------------- |
| Alt+F9 | 打开 / 关闭伤情测试模式   |
| F1     | 复位伤情并强制复活       |
| F2     | `Light_Arms`    |
| F3     | `Crippled_Legs` |
| F4     | 进入倒地 `Downed`   |
| F5     | 直接彻底死亡 `Dead`   |




### 在 Hierarchy 中快速搭建 Player（Editor 工具）

菜单 **Tools → Build Player in Hierarchy**（`Assets/Editor/BuildPlayerHierarchy.cs`）在当前打开的场景里拼一套完整 Player，**不保存 Prefab**，全程可 Undo。默认在 `Assets/Model/Soldier` 下找 redSolider 模型。

```
Player                    # Tag=Player，Layer=Player(7)
├── NetworkObject / ClientNetworkTransform / CharacterController / PlayerController
│   PlayerHealth / PlayerStatusController / PlayerWeapon / WeaponADS / ProceduralRecoil
├── ModelRoot             # redSolider 实例，已 UnpackPrefab 彻底解包
│   └── mixamorig:Hips …  # 骨骼 Hitbox：Head / Spine2 / LeftArm / RightArm / LeftUpLeg / RightUpLeg
│       └── mixamorig:RightHand
│           └── WeaponSocket   # ShellEjector；子物体为 GunMesh 或已挂枪模（Layer=Ignore Raycast）
│               └── SightPoint # Setup 菜单补；机瞄对齐用
└── PlayerCamera          # Camera + AudioListener 默认都关
    ├── muzzlePoint       # → PlayerWeapon.muzzlePoint
    └── CameraSightTarget # Setup 菜单补；屏幕中心对齐目标
```

要点：

- **骨骼 Hitbox**：`Head` / `Spine2` 用 `BoxCollider`，`LeftArm` / `RightArm` / `LeftUpLeg` / `RightUpLeg` 用 `CapsuleCollider`，尺寸按模型包围盒自动估算，部位写入 `BodyPartHitbox.bodyPart`。手改碰撞体后**不要再跑菜单**，否则会被重建覆盖。根节点的 `CharacterController` 不参与部位判定，射线会穿透它，见第 6 节。
- `CharacterMotor` **不是组件**：它是构造函数注入 `CharacterController` 的普通类，由 `PlayerController.Awake` 内部 `new`。菜单不会、也无法挂载它。
- **引用自动接线**：`PlayerController.playerCamera/audioListener/playerWeapon/weaponADS`、`PlayerWeapon.playerCamera`、`WeaponADS.sightPoint/weaponHolder`、`ProceduralRecoil.weaponHolder` 会被写入。完整 ADS（含 `cameraSightTarget`、正式枪模）请再跑 **Setup UpperBody Weapon and ADS**。
- **模型比例**：Mixamo FBX 若按厘米导入，模型会比 1.6 米胶囊大一圈，需手动调 `ModelRoot.localScale`。
- 场景里已有根 `Player` 时会弹窗确认替换；生成后自动选中并 `FrameSelected` 聚焦。要长期使用就把生成的 `Player` 拖成 Prefab。



### 为已选中的 Player 补挂组件（Editor 工具）

菜单 **Tools → Attach Components To Selected Player**（`Assets/Editor/AttachComponentsToPlayer.cs`）不新建对象，只对 **Hierarchy 中当前选中的 Player** 做「缺什么补什么 + 参数修正」，**幂等**，可反复执行。与 Build 菜单共用 `PlayerHierarchyUtils` 的装配逻辑。

- **根节点**：设 Tag `Player` / Layer 7；补齐 `NetworkObject` / `ClientNetworkTransform` / `CharacterController`(height 1.6, center 0) / `PlayerController` / `PlayerHealth` / `PlayerStatusController` / `PlayerWeapon` / `WeaponADS` / `ProceduralRecoil`。
- **权威修正**：发现服务器权威 `NetworkTransform` 会替换为 `ClientNetworkTransform`，并打警告。
- **模型定位**：优先取直接子节点 `ModelRoot`；没有就靠 `Head` 骨骼回溯到 Player 的直接子级。
- **武器挂点**：`mixamorig:RightHand` 下确保有 `WeaponSocket` → `GunMesh`（缺失则建空占位）；枪模整棵子树 Layer 设 Ignore Raycast、碰撞体一律禁用；`ShellEjector` 只挂一份（挂点或枪模已有就跳过）。
- **部位 Hitbox**：6 处骨骼按「世界米」定义尺寸、写入时折算到骨骼本地空间，模型被缩放也能保持预期大小。需要换碰撞体类型时，会先摘掉 `BodyPartHitbox`（它带 `[RequireComponent(typeof(Collider))]`，会保护碰撞体不让删），换完再挂回，保证一骨一体。
- **相机与引用**：确保 `PlayerCamera`（`Camera` + `AudioListener`，默认都关）与子节点 `muzzlePoint`，并统一接线 `PlayerController` / `PlayerWeapon` / `WeaponADS` / `ProceduralRecoil` 的引用。
- 选中的是 Prefab 实例时会提示「改动记为 Override，需 Apply 才写回 Prefab」。`PlayerHierarchyUtils.SetReference` 传 `null` 时直接跳过，**不会**清掉已有的正确引用。



### 枪械挂载与 ADS 对齐（Editor 工具）

菜单 **Tools → Setup UpperBody Weapon and ADS**（同文件 `AttachComponentsToPlayer.cs`）。支持 Hierarchy 里的 Player，或 Project 窗口选中的 `.prefab` 资产。

会幂等补全并接线：

```
mixamorig:RightHand
└── WeaponSocket
    └── 枪模（GunMesh 或已有 Mesh 子物体，如 M1 Garand Lowpoly）
        └── SightPoint          # 默认 local (0, 0.05, 0)，Scene 可拖
PlayerCamera
├── muzzlePoint                 # 开火点
└── CameraSightTarget           # 默认 local (0, 0, 0.4)，屏幕中心对齐点
```

硬性规范（与框架一致）：

- 枪模整棵子树 **Layer = Ignore Raycast (2)**
- **销毁**枪模子树上所有 Collider（防挡子弹 / 误进战区 Trigger）
- `WeaponADS` 挂在 **Player 根**（与 `PlayerController` 一致）：`weaponHolder`→枪模，`sightPoint`→`SightPoint`，`cameraSightTarget`→`CameraSightTarget`

完成后 Console：`【已成功补全枪械挂载与 ADS 节点，已自动清洗 Layer 与 Collider！】`



### 推荐 Player 预制体组件

- `NetworkObject`
- `CharacterController`（height 1.6，center 0）
- `PlayerController`（`standingHeight = 1.6`）
- `ClientNetworkTransform`（不要给玩家挂服务器权威 NetworkTransform）
- `PlayerHealth` / `PlayerStatusController`
- `PlayerWeapon` / `WeaponADS` / `ProceduralRecoil`
- `PlayerAnimationManager`
- 子物体 `ModelRoot`（`Animator` + `PlayerAnimatorController` + redSolider Avatar，`applyRootMotion=false`，`localPosition.y ≈ -0.8`）+ 6 处骨骼 Hitbox
- 子物体 `…/RightHand/WeaponSocket` → 枪模（如 `M1 Garand Lowpoly`）+ `SightPoint`；Layer = Ignore Raycast，无 Collider
- 子物体 `PlayerCamera`（默认关）+ `muzzlePoint` + `CameraSightTarget`

`PlayerWeapon.bulletPrefab` 须绑 `Bullet.prefab`。当前主预制体 **`gunSound` 仍为空**，需补正式枪声 Clip。

进入第一人称时 `SetViewEnabled`：**仅** `IsOwner` 打开相机并独占 `AudioListener`。`Player.prefab` 上 Listener 默认禁用。枪声用 Inspector 拖的 `AudioClip`，不要读磁盘路径 `Assets/Source/Audio/gun.wav`。

枪模子树：Collider 全关，Layer = **Ignore Raycast**，否则挡子弹 / 误进战区 Trigger。

### 动画（摘要）

- 资产：`Assets/Animations/PlayerAnimatorController.controller` + `UpperBodyMask.mask`
- `PlayerAnimationManager` 写 `InputX/Y`、`IsGround`、`IsADS`、`Fire`/`Reload`/`Jump`；摄像机只跟 Head **位置**
- Base Layer：`Locomotion` 混合树；UpperBody：Empty ↔ Fire/Reload/ADS
- **Jump 只有参数、无状态**，跳跃动画暂不播；蓝方需把 `blueSolider` 切 Humanoid

相关菜单：`Tools → Auto Bind Player Animator` / `Check Player Animator Setup` / `Fix ADS Fire Transitions` / `Validate and Fix Animation Settings`

---



## 5. Managers（出生与复活）


| 文件                | 作用                                 |
| ----------------- | ---------------------------------- |
| `SpawnManager.cs` | 批准进房但不自动生成玩家；选阵营后 `SpawnForClient` |


场景里挂 `SpawnManager`（`NetworkBehaviour`），配置红/蓝 `BoxCollider` 复活区。算法：区内随机点 + `Physics.CheckSphere` 防卡模，失败则用区域中心。

`SpawnForClient(TeamId team, ulong clientId)` 返回 `bool`（出生区缺失等失败为 false）：

1. 没有 PlayerObject → `Instantiate` + `SpawnAsPlayerObject`
2. 已有（死亡复活）→ 不在服务器硬写 Owner 坐标
3. `PersistTeam` + `RememberSpawnTeam`
4. Targeted `NotifyPlayerSpawnedClientRpc` → Owner `TeleportToSpawn` + `CombatStatusUI.Hide` + 关选阵营 + `GameplayGate.Release`
5. 若仍是 Dead → `Revive()` 回满血

对局中重连：若会话里还记着该客户端的阵营，直接 `SpawnForClient`，不把 Host 拉回大厅。

---



## 6. Enemy（练习假人）

伤情枚举已迁到 `Core/InjuryState.cs`。假人脚本 `using Core`。


| 文件                       | 作用                                    |
| ------------------------ | ------------------------------------- |
| `BodyPartHitbox.cs`      | 部位盒；沿射线挑最精确部位（穿透 CharacterController） |
| `PracticeDummyHealth.cs` | 假人血量、伤情、镜面反弹、爆头后延时复位                  |
| `PracticeDummyHud.cs`    | 假人头顶 Debug 血量 + 伤情                    |


预制体：`Assets/Player/TestEnemy/PracticeDummy.prefab`。

### 部位 → 伤情 → 扣血（假人）


| `DetailedBodyPart` | 伤情                   | 假人扣血 |
| ------------------ | -------------------- | ---- |
| Head               | `InstanceDeath_Head` | 直接 0 |
| Torso              | `DBNO_Torso`         | −50  |
| Legs               | `Crippled_Legs`      | −25  |
| Arms               | `Light_Arms`         | −15  |


躯干盒常常盖住头和手脚。Hitscan / 实弹都用 `RaycastNonAlloc` + `BodyPartHitbox.PickBestAlongRay`：优先级 **Head > Arms/Legs > Torso**。

**CharacterController 必须被「穿透」**：CC 挂在 Player 根节点、本身没有 `BodyPartHitbox`，而运行时半径会被 `PlayerController` 改写成 0.5（比头盒 0.2、大腿胶囊都大），射线按距离排序后它必定位列第 0。`PickBestAlongRay` 遇到 `col is CharacterController` 时执行 `continue` **跳过**（**绝不能用** `break`），否则会立刻中断扫描、让整套部位判定返回空——表现就是「打得中却永远不爆头、弹孔浮在模型体外」。`break` 的「范围哨兵」语义只保留给真正的非 Hitbox 物体（墙 / 地面 / 站在目标身后的人），用来防止把人家的头算到别人身上。

`PickBestInOverlaps` 不做这个跳过：重叠查询没有射线顺序，CC 与骨骼 Hitbox 同时命中时按「具体度」取最精确的那个即可。

打到**真实玩家**时：普通部位 HP 到 0 → `Downed`；**爆头** `instantKill` → 直接 `Dead`。

爆头判定**只算一次**：`isHeadshot` 由 `PickBestAlongRay` 选出的 `bestHitbox` 得出，并透传给伤害结算（`ApplyHitscanPlayerDamage(hitCount, bestHitbox)` / `ProcessHit(hit, hitbox)`）。**不要**再在结算里「取最近命中」重算爆头——最近命中往往是 CC 或躯干盒，重算会把爆头结论抹掉。结算同时校验 `bestHitbox` 与最近受击者属于同一个 `PlayerHealth`，防止射线穿透多人时算错对象。

`BodyPartHitbox.TakeHit` / `OnHit` 是**假人专用**反馈（内部找 `PracticeDummyHealth`）。真人身上没有该组件，因此调用前必须先判 `hitbox.GetComponentInParent<PracticeDummyHealth>() != null`，否则一旦部位判定恢复，打真人每枪都会刷「父级未找到 PracticeDummyHealth」警告。

### 镜面反弹（单机验证 Debuff）

假人命中会把伤情写到正在操控的本地玩家。爆头后约 3 秒假人复位。这是练习工具，正式 1v1 走 `PlayerHealth` 状态机。

---



## 7. Weapon（射击与受击反馈）


| 文件                    | 作用                                                                 |
| --------------------- | ------------------------------------------------------------------ |
| `PlayerWeapon.cs`     | Hitscan / Projectile；RPC + 本地权威；命中后广播 VFX                          |
| `Projectile.cs`       | 服务器模拟扫掠弹道；命中 ClientRpc VFX 后 `Despawn`                             |
| `HitEffectSpawner.cs` | 弹孔 / 粒子；`SpawnHitEffectAt(point, normal, isHeadshot)` 不依赖 Collider |
| `WeaponADS.cs`        | 右键机瞄：腰射用 `hipfirePosition`；ADS 时让 `SightPoint` 对齐 `CameraSightTarget` + 缩 FOV |
| `ProceduralRecoil.cs` | 程序化后坐力（枪 + 镜头）                                                     |
| `ShellEjector.cs`     | 抛壳                                                                 |
| `TimedDestroy.cs`     | 弹壳等限时销毁                                                            |


场景 **HitEffectManager** 挂 `HitEffectSpawner`，拖好 `Quad.prefab`（可选 `fireSound` 作后备）。枪声主 Clip 在 `PlayerWeapon.gunSound`。弹孔 Prefab **不要**放进 NetworkPrefabs。

### WeaponADS 对齐约定

| 引用 | 节点 | 作用 |
| --- | --- | --- |
| `weaponHolder` | 枪模 Transform（如 GunMesh / M1 Garand） | 被脚本改 local 位姿 |
| `sightPoint` | 枪上 `SightPoint` | 照门/准星 |
| `cameraSightTarget` | `PlayerCamera/CameraSightTarget` | 屏幕中心对齐目标（默认前方 0.4m） |
| `playerCamera` | `PlayerCamera` | FOV 插值 |

有 `sightPoint` + `cameraSightTarget` 时：ADS 移动 holder，使两节点世界坐标重合。缺 `cameraSightTarget` 时退回旧的视线偏移写法（兼容旧预制体）。腰射 / 机瞄姿态可在 Scene 里拖 `SightPoint` / `CameraSightTarget` / `hipfirePosition` 微调。

### 双路径权威

`CanSendWeaponRpc`：

```
IsSpawned && NetworkManager.Singleton != null && NetworkManager.IsListening
```


| 模式               | 射击路径                    | 子弹                                                  |
| ---------------- | ----------------------- | --------------------------------------------------- |
| **联机**（已 Spawn）  | `[ServerRpc]` → 服务端判定伤害 | 服务器 `NetworkObject.Spawn()`，`NetworkTransform` 同步飞行 |
| **尚未 Listening** | 本地权威                    | 仅 `Instantiate`                                     |


伤害只在服务器结算。命中特效用 `BroadcastHitVfxClientRpc(Vector3, Vector3, bool)`，各客户端本地 Instantiate。不要把 `RaycastHit` / `Collider` 塞进 RPC。

### Hitscan

1. 左键 → `ShootHitscan`
2. 能 RPC → `SubmitHitscanServerRpc`；否则本地 `ExecuteHitscan`
3. `RaycastNonAlloc`，`hitLayers` 含 Ground
4. 忽略自己的碰撞体
5. `PickBestAlongRay` 穿透 CC 选出 `bestHitbox` → 全流程唯一一份 `isHeadshot`
6. 假人门闸：仅 `GetComponentInParent<PracticeDummyHealth>() != null` 才 `TakeHit("Hitscan")`
7. 特效坐标取「该 Hitbox 自己的那次命中」，不是最近命中（否则弹孔落在 CC 表面、浮在体外）
8. `ApplyHitscanPlayerDamage(valid, bestHitbox)`：找最近 `PlayerHealth`，复用第 5 步的爆头结论



### Projectile

1. 按 **2** 切实体弹 → 服务器生成 `Bullet.prefab`
2. **运动学扫掠**：每帧从上一帧位置射线到新位置（高速不穿模）
3. 刚体 kinematic；自身 Collider 关掉
4. `TrySweepHit` 同样走 `PickBestAlongRay`，把 `bestHitbox` 一并带出给 `ProcessHit`
5. 命中：`ProcessHit(hit, hitbox)` 复用同一份爆头结论 + 假人门闸；VFX 用 Hitbox 自身命中点 → ClientRpc VFX → `Despawn()`



### 子弹预制体 `Assets/Player/Bullet.prefab`


| 组件                 | 说明                                    |
| ------------------ | ------------------------------------- |
| `NetworkObject`    | 联机 Spawn                              |
| `NetworkTransform` | **服务器权威**（不要用 ClientNetworkTransform） |
| `Projectile`       | 扫掠弹道                                  |
| `Rigidbody`        | kinematic，脚本驱动                        |




### 弹孔与枪声

- Quad 用 `LookRotation(-normal)`，沿法线抬出约 `0.03`，世界缩放约 `0.12`，不挂到缩放过的 Hitbox 上
- 命中不播枪声；枪声只在 Owner 本地 `PerformShoot` 时播一次（`gunSound` 拖到 `PlayerWeapon`）
- `HitEffectSpawner` 不再从 `Application.dataPath` 解码 wav；缺 Clip 时用占位蜂鸣。音频排查看 `开枪音效排查笔记.md`

---



## 8. World（线性 5 战区占领 + 局内结算）

对应第四阶段第二步 / 第三步：**A 红 HQ — B — C — D — E 蓝 HQ**。`SectorManager` **不是单例**，场景 5 个实例，靠 `sectorId` + 左右邻居链表工作。

圈外网格目前只做**驻留查询**（给以后兵种部署用），**不**加占领权重。推动进度的只有核心圈 Alive 人数 × `captureWeight`（场景里多为 3），且该阵营须满足「可进圈」（进攻已解锁 **或** 本区已是己方占领可防守）。

已落地：严格邻接解锁、`HasBeenCaptured`、五格顶栏 HUD、倒计时与推平/限时结算。尚未做：圈外网格加权、Warfare/Offensive 模式状态机、Tickets、倒地队友救援。


| 文件                       | 作用                                                                                                         |
| ------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `SectorData.cs`          | 配置：`pointId` / `pointName` / `defaultTeam` / `captureDuration` / `emptyDecayPerSecond` / `HasBeenCaptured` |
| `GridSectorArea.cs`      | 大战区 Box Trigger；只记谁在格子里，不参与占领权重                                                                            |
| `StrongpointArea.cs`     | 核心圈 Sphere Trigger；Alive 红/蓝战力 = 人数 × `captureWeight`；Debug 可增减 Mock 人数                                    |
| `SectorManager.cs`       | **实例级** `NetworkBehaviour`：邻接过滤 + 写 `OwnerTeam` / `CaptureProgress` / `HasBeenCaptured` / 圈内战力 NV          |
| `SectorCaptureRules.cs`  | 纯规则（邻接、易主、CaptureRate、HUD 填色、进圈/防守权限），运行时与白盒单测共用                                                           |
| `SectorTriggerFilter.cs` | Trigger 统一过滤：只认根节点 `CharacterController` + Tag `Player`                                                    |
| `MatchGameManager.cs`    | 局内倒计时（默认 900s）+ 推平/限时结算；挂在对局场景（如 SpawnManager 同物体）                                                         |
| `MatchOutcomeRules.cs`   | 胜负纯函数：红占 E / 蓝占 A；限时比占领数                                                                                   |
| `SectorArea.cs`          | **旧脚本**，场景已不用；新逻辑走 Grid + Strongpoint                                                                      |


场景节点（`Testcene_GamePlay`）：

```
[Sectors]
  ├── Sector_A  红方指挥部   defaultOwner=Red    左无 / 右 B
  ├── Sector_B  农庄前哨     defaultOwner=None   左 A / 右 C
  ├── Sector_C  中央教堂     defaultOwner=None   左 B / 右 D
  ├── Sector_D  桥头堡       defaultOwner=None   左 C / 右 E
  └── Sector_E  蓝方指挥部   defaultOwner=Blue   左 D / 右无
```

每个 Sector：根物体挂 `NetworkObject` + `SectorManager` + `GridSectorArea` + BoxCollider（Trigger）；子物体挂 `StrongpointArea` + SphereCollider（Trigger）。

### 8.1 Trigger 过滤（防枪模算人头）

`GridSectorArea` / `StrongpointArea` 都走 `SectorTriggerFilter`：

1. `other` **必须是** `CharacterController`（不是子物体 Collider）
2. `CompareTag("Player")`
3. 正式玩家：同物体 `PlayerController` + 已 Spawn + 仅 Server
4. Editor 黑盒 Dummy：可挂 `SectorTestDummy`，无 NGO 也可计入测试集合

**严禁** `GetComponentInParent<PlayerController>()`，否则枪模 / 子弹 Hitbox 会进 `_playersInZone`。

### 8.2 严格邻接解锁（进攻）与防守进圈

**进攻解锁** `IsUnlockedFor` / `IsActiveFrontlineFor`：该阵营可在本区推进，当且仅当

- 左邻居 **或** 右邻居已被该阵营占领

**不因**「本区已是己方占领」而自动解锁进攻（后方 HQ 靠邻接保护：B 仍属红时，蓝打不了 A）。邻居 **未 Spawn** 时用 `DefaultOwner`，禁止读未同步的 NetworkVariable。

开局：B 对红解锁、D 对蓝解锁，**C 对双方锁定**，直到 B 被红占（红可打 C）和/或 D 被蓝占（蓝可打 C）。

**进圈 / 防守** `CanOccupyOrDefend`：与进攻解耦。

- 本区 `Owner == team`（含 A/E HQ）→ **永远允许**进人并计入战力（防守）
- 否则 → 必须已进攻解锁

灰盒 F6/F7 模拟人数走 `CanOccupyOrDefend`；F8/F9/F10 改进度仍走 `IsUnlockedFor`。**禁止**再 `DebugSetIgnoreFrontline(true)`（调用会被拒绝）。

### 8.3 有效战力与进度

圈内物理在场 ≠ 能占点。`EvaluateActivePlayers` 只统计：

```
PlayerHealth.LifeState == PlayerLifeState.Alive
```

`Downed` / `Dead` 不计。位置已不在 Sphere bounds 内会清幽灵 ClientId。掉线：每个已 Spawn 的 Manager 订 `OnClientDisconnectCallback`。

服务端每帧（`IsServer && IsSpawned`，且对局未结束）：

```
EvaluateActivePlayers → CanOccupyOrDefend 过滤 → 同步 OccupantRed/BlueCount
rate = (red - blue) / captureDuration     // 无人则 0；可选 emptyDecayPerSecond 向 0 回落
CaptureProgress = Clamp(progress + rate * dt, -1, 1)
打满 ±1 → Owner 易主，并闩上 HasBeenCaptured
```

人数相等则僵持。日志（战力变化立刻打，占领中约 0.25s 一条）：

`[Game][Sector] Sector_C 实时战力: Red=3, Blue=0, Progress=0.120`

### 8.4 易主与 HasBeenCaptured

`CaptureProgress`：`-1` 蓝满 ←→ `0` 中立线 ←→ `1` 红满。

- 只有 **≥ +1** 才把 `OwnerTeam` 设为 Red
- 只有 **≤ -1** 才设为 Blue
- 进度拉回 `0` **保持原归属**，绝不写成 `None`
- 首次打满 ±1 → `HasBeenCaptured = true`（网络同步，且不再清回 false；开局 A/E 会立刻闩上）

读写权限：`Everyone` 读、`Server` 写。覆盖 `OnDestroy` 必须 `override` + `base.OnDestroy()`。

公开只读：`SectorId` / `PointName` / `DefaultOwner` / `IsRearGuarded` / `CanOccupyOrDefend` 等。

### 8.5 顶栏 HUD（`UI/SectorHUDUI.cs`）

运行时生成：正中 `MatchTimerText` + `SectorChain`（Block_A~E）。每格：`GrayFill` + `RedFill`(Origin=Left) + `BlueFill`(Origin=Right) + 锁标 + 徽章。旧场景里的 RedBar/BlueBar 等会隐藏。


| 状态                                   | 画法                               |
| ------------------------------------ | -------------------------------- |
| 上锁 / 后方保护（中立且不可争夺，或 `IsRearGuarded`） | 所属阵营**纯色** + 锁标；**无深色遮罩**        |
| 双方都在圈内（`RedCount>0 && BlueCount>0`）  | **强制**红蓝拼接，消灭灰底（开局 C 双人入场也如此）    |
| 从未占领且单方/无人                           | 灰底，红/蓝自两侧向中间填                    |
| `HasBeenCaptured` 或已有归属              | **永久不露灰**；整格红底 + 蓝条自右覆盖，分界线随进度移动 |


倒计时读 `MatchGameManager.MatchTimer`（`MM:SS` / `H:MM:SS`）。

### 8.6 局内胜负（`MatchGameManager`）


| 条件              | 结果                      |
| --------------- | ----------------------- |
| 红方占领 `Sector_E` | 红方推平胜                   |
| 蓝方占领 `Sector_A` | 蓝方推平胜                   |
| 倒计时到 0          | 比 `OwnerTeam` 占领数；相等平局  |
| 结算后             | `MatchEndUI` 提示，延迟回大厅场景 |


对局结束后 `MatchGameManager.IsMatchOver` 为真，各 `SectorManager` 不再推进度。

### 8.7 测试体系（`Assets/Tests/`）


| 类型     | 入口                                          | 做什么                                                          |
| ------ | ------------------------------------------- | ------------------------------------------------------------ |
| **白盒** | Test Runner → EditMode → `SectorLogicTests` | 邻接链、A 保护、C 双方、易主、CaptureRate、HUD/`HasBeenCaptured`、进圈防守、枪模拒绝 |
| **灰盒** | Play 后左上角面板 / Inspector                     | Mock 增减圈内人数、半进度/临界、强制易主、限时结算                                 |
| **黑盒** | 面板按钮或 **F5**                                | Dummy（CC）与 GunMesh 穿过 Trigger，断言只有身体计入                       |


Editor 打开 `Testcene_GamePlay` 直接 **Play**：未监听则 Harness **Auto-Host**（UnityTransport）。已从大厅进房则跳过。

热键（**不需要** Alt+F9；选区用**小键盘** 1–5，主键盘 1/2 仍是切枪）：


| 键             | 作用                               |
| ------------- | -------------------------------- |
| 小键盘 1–5       | 选中 A–E                           |
| F5            | 黑盒物理 Trigger                     |
| F6 / Shift+F6 | 红方模拟人数 +1 / -1（己方占领区可防守，否则须进攻解锁） |
| F7 / Shift+F7 | 蓝方模拟人数 +1 / -1                   |
| F8 / Shift+F8 | 进度 +0.5 / -0.5（须进攻解锁；不改归属除非 ±1）  |
| F9 / Shift+F9 | 进度 +0.95 / -0.95                 |
| F10           | 红方打满 +1 并易主                      |
| F11           | 进度清 0（保持归属；须已可争夺）                |
| F12           | Debug 限时立刻结算                     |


控制台统一 `[Game][SectorTest] [PASS]/[FAIL]`。离开圈用 GUI「模拟死亡/传送离开」，不再占 F8。

---



## 9. SteamConnect（联机）


| 文件                         | 作用                                 |
| -------------------------- | ---------------------------------- |
| `SteamManager.cs`          | SteamAPI 初始化与 `RunCallbacks`       |
| `SteamLobbySession.cs`     | 大厅会话、切场景、`_gameplayStarted`、阵营表    |
| `SteamLobbyUI.cs`          | 开房、邀请码、加入、单机练习                     |
| `SteamNetworkTransport.cs` | NGO 传输层，SteamNetworkingSockets P2P |
| `JoinCodeUtility.cs`       | 6 位邀请码（去掉易混的 0/O、1/I）              |
| `LobbySessionState.cs`     | 会话状态枚举                             |
| `SteamLobbyErrors.cs`      | Steam 错误码中文提示                      |




### 关键标志 `_gameplayStarted`

对局真正开始后：

- 远程客户端连上 **禁止** 再触发全局 `NetworkStarted`（否则 Host 会被拉回选阵营）
- `TryStartMatch` / `LoadMatchAndSpawn`：**已在对局场景则禁止二次** `LoadScene`
- 死亡复活只 `SpawnForClient`，不重新选阵营
- 重连：若 `_chosenTeams` 里还有该客户端的阵营，自动复活；否则只给该客户端弹选阵营

单机：`StartOfflineHost()` 用 `UnityTransport`。联机：`SteamNetworkTransport`。`MaxClients = 1`（1v1：Host + 1 个 Client）。断开时要从连接表里移除，否则重连进不去。

### 短邀请码

创建房间时把 6 位码写入大厅数据 `join_code`；加入方按码 `RequestLobbyList` 过滤后再 `JoinLobby`。

---



## 10. UI（界面）

**正在用的选阵营**是 `Assets/Scripts/UI/FactionSelectUI.cs`（`namespace UI`），运行时 Overlay，`sortingOrder = 100`。场景 Canvas 默认 0 会被挡住，所以不能靠场景里那两个旧按钮。

`Assets/Scripts/UIinteraction/FactionSelectUI.cs` 是旧转发器，只把调用转到新 UI，不要再往里面加逻辑。


| 文件                                  | 作用                                               |
| ----------------------------------- | ------------------------------------------------ |
| `UI/FactionSelectUI.cs`             | 首次进房选红蓝（按钮传 `TeamId.Red` / `Blue`）               |
| `UI/CombatStatusUI.cs`              | 倒地 HUD + 死亡黑屏（运行时 DDOL）；`Hide()` 由出生 RPC 调用      |
| `UI/SectorHUDUI.cs`                 | 顶栏：倒计时 + A~E 五格（锁 / 灰底或红蓝拼接）；读各区 NV + MatchTimer |
| `UI/MatchEndUI.cs`                  | 推平 / 限时结算提示（由 MatchGameManager 触发）               |
| `UIinteraction/RuntimeUiFactory.cs` | Canvas / 按钮 / 俯视相机 / 独占 AudioListener            |
| `UIinteraction/PauseMenuUI.cs`      | Esc：继续 / 返回大厅                                    |



| UI      | sortingOrder |
| ------- | ------------ |
| 大厅      | 20           |
| 倒地 / 死亡 | 90           |
| 选阵营     | 100          |
| 暂停      | 110          |


大厅 / 首次选阵营用俯视 `LobbyOverviewCamera`；真正开打后关掉它。死亡黑屏是 Overlay，不需要再开俯视相机。

---



## 11. 场景与层级


| 场景                  | 作用                                                                         |
| ------------------- | -------------------------------------------------------------------------- |
| `Testcene`          | 启动、大厅、单机练习、假人                                                              |
| `Testcene_GamePlay` | Steam 联机对局（SpawnManager 复活区 + 线性 5 个 SectorManager）                        |
| `ModelScene`        | 搭角色 / 跑 **Tools → Build Player in Hierarchy** 的开发场景，**不勾进 Build Settings** |



| 物体                       | 作用                                                                                                                 |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| SpawnManager             | 红/蓝 BoxCollider 复活区；同物体或场景内挂 `MatchGameManager`                                                                    |
| `[Sectors]` / Sector_A~E | 每区：`NetworkObject` + `SectorManager` + `GridSectorArea` + 子物体 `StrongpointArea`                                    |
| SectorHUD                | 挂 `SectorHUDUI`；运行时生成五格链条与倒计时（可隐藏旧 RedBar 等）                                                                       |
| Terrain                  | 层 **Ground（6）**，Tag `Ground`                                                                                       |
| HitEffectManager         | `HitEffectSpawner`                                                                                                 |
| Practice Dummy           | 部位盒 + HUD（练习场景）                                                                                                    |
| NetworkManager           | `Network Prefabs Lists` → `GameList`；**Testcene** PlayerPrefab → `Player.prefab`；**Testcene_GamePlay** 仍 → `Player Test.prefab`（待统一）；联机 Steam 传输，单机切 UnityTransport |



| 层              | 编号  | 谁在用                                                  |
| -------------- | --- | ---------------------------------------------------- |
| Default        | 0   | 假人、环境等                                               |
| Ignore Raycast | 2   | 枪模子树（如 M1 Garand / GunMesh；不挡子弹、不进占点 Trigger） |
| Ground         | 6   | 地形                                                   |
| Player         | 7   | 玩家根物体 + 角色模型全身（贴地射线掩码是 `~(1 << 7)`，避免检测打到自己的 Hitbox） |


射线与实弹的 `hitLayers` 都应能打到 Ground，否则只有假人有弹孔、地板没有。

---



## 12. NGO 编写约定

与 `.cursorrules` 一致：

1. **禁止** UNet（`[Command]`、`NetworkIdentity` 等）
2. 网络脚本继承 `NetworkBehaviour`
3. 状态同步用 `NetworkVariable<T>`，显式声明 `Everyone` 读 / `Server` 写；赋值前判断 `IsSpawned` / `IsServer`
4. 客户端请求：`[ServerRpc]`；服务器广播：`[ClientRpc]`（方法名必须以对应后缀结尾）
5. 输入与移动：`if (!IsOwner) return;`
6. 生成网络物体：`NetworkObject.Spawn()` / `SpawnAsPlayerObject`（仅已注册预制体）
7. RPC 参数只能是可序列化类型（`Vector3`、`int`、`bool`、`TeamId` 的 int…）。**禁止**传 `Collider` / `GameObject` / `Transform` / `RaycastHit`
8. `OnNetworkSpawn` 订阅的回调必须在 `OnNetworkDespawn` / `OnDestroy` 反订阅。覆盖 `NetworkBehaviour.OnDestroy` 时必须 `override` 并 `base.OnDestroy()`
9. 日志用 `GameLog.Info` / `Warn` / `Error`，不要散落 `Debug.Log`



### 网络预制体

- `Assets/GameList.asset`：含 `Player.prefab` + `Bullet.prefab`
- `Assets/DefaultNetworkPrefabs.asset`：同时列了 `Player.prefab` 与 `Player Test.prefab`
- 弹孔、粒子、抛壳 **不要** 注册成网络预制体
- 联机前建议把 `Testcene_GamePlay` 的 PlayerPrefab 统一到 `Player.prefab`



### C# 命名

- 类 / 属性 / 方法：PascalCase
- 参数与局部变量：lowerCamelCase
- 私有字段建议：`_moveSpeed`

---



## 13. 扩展建议


| 需求                  | 建议落点                                                                     |
| ------------------- | ------------------------------------------------------------------------ |
| 队友按 E 救援倒地玩家        | 新脚本看 `PlayerLifeState.Downed`，服务器改回 Alive；不要塞进 UI                        |
| Tickets 与占领扣费       | 结合复活次数，不要写进 HUD；可挂在 `MatchGameManager` 旁                                 |
| 圈外网格 1 倍权重叠加入核心圈    | 在现有 `GridSectorArea` + `SectorManager.EvaluateFrontlineForces` 上加，不要另起计数 |
| 攻防模式（Offensive）额外锁定 | 在 `IsActiveFrontlineFor` / `IsRearGuarded` 上扩展，不要复制一套 Manager            |
| 正式击中音（血肉 / 头盔）      | `SpawnHitEffectAt` 里按 `isHeadshot` 播，与左键枪声分开                             |
| 微调机瞄姿态               | Scene 拖 `SightPoint` / `CameraSightTarget`；或改 `WeaponADS.hipfirePosition`   |
| 统一联机场景 PlayerPrefab  | `Testcene_GamePlay` 从 `Player Test.prefab` 改到 `Player.prefab`                 |
| 跳跃状态机 / 蓝方 Avatar  | Base Layer Jump 三段 + `blueSolider` 切 Humanoid                                |
| 绑定枪声 Clip             | `PlayerWeapon.gunSound`（当前主预制体为空）                                          |
| 换新输入系统              | 主要改 `GameplayInputState.Read()`                                          |
| 正式 Steam AppID      | 替换 `480` 与 `steam_appid.txt`                                             |


---



## 14. 快速自检清单

> `[x]` = 代码/资产已核对；`[ ]` = 缺实现或需实机确认。

**工程与网络**

- [x] Steam 测试 AppID 480；Build Settings 勾了 `Testcene` + `Testcene_GamePlay`
- [x] 联机 `SteamNetworkTransport`；单机练习切 `UnityTransport`
- [x] `GameList` 含 `Player.prefab` + `Bullet.prefab`
- [x] 对局场景有 SpawnManager 红/蓝区 + 5 Sector + MatchGameManager
- [ ] `Testcene_GamePlay` PlayerPrefab 仍为 `Player Test.prefab`，待统一
- [x] Esc 暂停可回大厅
- [ ] 对手中途重连：Host 不回大厅（需联机实操）

**Player 预制体**

- [x] 主预制体 `Player.prefab`；已挂 M1 Garand；`WeaponSocket` / `SightPoint` / `CameraSightTarget` 齐全
- [x] 枪模 Layer = Ignore Raycast；Collider 已清（可用 **Setup UpperBody Weapon and ADS** 重跑校验）
- [x] `WeaponADS`：`weaponHolder` / `sightPoint` / `cameraSightTarget` 已接线
- [x] `standingHeight` / CC height / ModelRoot.y 统一为约 1.6 / -0.8
- [x] `PlayerAnimationManager` + Animator（Controller + Avatar，`applyRootMotion=false`）
- [x] `bulletPrefab` 已绑；**`gunSound` 为空**（待补）
- [ ] 实机确认：右键机瞄准星对屏幕中心；腰射姿态自然
- [ ] 跳跃动画未接；`blueSolider` 仍 Generic

**射击 / 战区**

- [x] Hitscan 穿透 CC；爆头结论只算一次；假人有 PracticeDummyHealth 门闸
- [x] 倒地 / 死亡 / 自动复活（等 Spawn RPC）
- [x] 战区邻接解锁 + 五格 HUD + 推平/限时；EditMode `SectorLogicTests` 可跑

---



## 15. 关键脚本索引

```
Core/
  TeamId.cs                # TeamId + TeamIdUtil
  InjuryState.cs           # 从 Enemy 迁入，玩家/假人共用
  GameLog.cs
  GameplayGate.cs
  PauseGate.cs
  GameplayInputState.cs
  PlayerRegistry.cs
  SteamRuntime.cs
  GameScenes.cs
  SceneHubGuard.cs
  DebugCommandGate.cs      # Alt+F9 伤情测试开关

Player/
  PlayerController.cs
  CharacterMotor.cs
  FirstPersonLook.cs
  ClientNetworkTransform.cs
  PlayerHealth.cs
  PlayerStatusController.cs
  PlayerAnimationManager.cs

Managers/
  SpawnManager.cs

World/
  SectorData.cs / GridSectorArea.cs / StrongpointArea.cs / SectorManager.cs
  SectorCaptureRules.cs / SectorTriggerFilter.cs
  MatchGameManager.cs / MatchOutcomeRules.cs
  SectorArea.cs            # 旧脚本，场景已不用

Weapon/
  PlayerWeapon.cs / Projectile.cs / HitEffectSpawner.cs
  WeaponADS.cs / ProceduralRecoil.cs / ShellEjector.cs / TimedDestroy.cs

Enemy/
  BodyPartHitbox.cs / PracticeDummyHealth.cs / PracticeDummyHud.cs

SteamConnect/
  SteamLobbySession.cs / SteamLobbyUI.cs / SteamNetworkTransport.cs
  JoinCodeUtility.cs / LobbySessionState.cs / SteamLobbyErrors.cs

UI/
  FactionSelectUI.cs / CombatStatusUI.cs / SectorHUDUI.cs / MatchEndUI.cs

UIinteraction/
  RuntimeUiFactory.cs / PauseMenuUI.cs
  FactionSelectUI.cs   # 旧转发器

Tests/
  Editor/SectorLogicTests.cs / SectorTestHarnessEditor.cs
  Runtime/SectorTestHarness.cs / SectorPhysicsTest.cs / SectorTestDummy.cs / SectorTestReport.cs

Editor/
  BuildPlayerHierarchy.cs / AttachComponentsToPlayer.cs / PlayerHierarchyUtils.cs
  # AttachComponents 含：Attach Components + Setup UpperBody Weapon and ADS
  PlayerAnimatorBinder.cs / AnimatorSetupChecker.cs / AnimatorAdsFireFixer.cs
  AnimationSettingsValidator.cs
  RefreshNetworkPrefabIds.cs / SteamBuildCopyAppId.cs

Animations/
  PlayerAnimatorController.controller
  UpperBodyMask.mask

Steamworks.NET/
  SteamManager.cs
```

---

*按当前仓库整理：Steam 1v1、延迟出生、倒地死亡自动复活、线性 5 战区、Hitscan/Projectile、动画驱动已接入。主预制体* `Player.prefab`*（已挂 M1 Garand + SightPoint / CameraSightTarget；* `WeaponADS` *屏幕中心对齐）；联机场景仍用* `Player Test.prefab`*。装配菜单：Build / Attach Components / **Setup UpperBody Weapon and ADS**。跳跃状态与蓝方 Avatar 未齐、`gunSound` 待绑。预制体结构看* `Player预制体结构说明书.md`*；音频看* `开枪音效排查笔记.md`*；近期计划看* `接下来三天的开发日程.md`*。*