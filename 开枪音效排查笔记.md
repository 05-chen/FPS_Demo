# 开枪音效排查笔记

**现象：** Hitscan / Projectile 已经能打出弹孔（中弹画面有了），但听不到开枪声或击中音效。

**相关代码：** `PlayerWeapon.cs` · `HitEffectSpawner.cs` · 场景物体 `HitEffectManager` · `Assets/Source/Audio/gun.wav`

**相关知识点：** `AudioClip`（音频资源）、`AudioSource`（扬声器）、`AudioListener`（耳朵）。Unity 里必须三者齐全：有 Clip、有人播、场景里有且仅有一个启用的 Listener，扬声器才会出声。

**更关键的一层：** Console 打出“正在播放”**不等于**扬声器出声。`PlayOneShot` 只表示“把 Clip 交给音频引擎了”。引擎静音、导入器把 Clip 解成空数据、或 Windows 输出设备不对时，日志照样成功、耳朵里什么都没有。

---

## 1. 先看日志，分清“没调用”还是“调用了但没声”

开枪时看 Console。当前版本第一枪还会多一行诊断。

```
[GunShot] 已绕过导入器，直接解码 .../Assets/Source/Audio/gun.wav（采样 / Hz）
[GunShot] 诊断: InspectorClip=(...), 实际播放=gun_pcm16/..., Listener启用数=1(PlayerCamera), editorMute=False, ...
[GunShot] 播放音效: gun_pcm16, 采样=..., 声源: PlayerCamera
```


| 日志                                              | 含义                                                                               |
| ----------------------------------------------- | -------------------------------------------------------------------------------- |
| 完全没有 `[GunShot]`                                | `PlayFireSound` / `PlayHitSound` 没被调用：开火输入、冷却、或 `PerformShoot` 没走到播声音            |
| `[HitEffectSpawner] 音效 Clip 未赋值` / 没有可播放的音效     | 文件、Inspector Clip、占位蜂鸣都失败                                                        |
| `[GunShot] 播放音效: gun, 声源: PlayerCamera` **但没声** | **曾经踩过的坑**：代码已 `PlayOneShot`，声源也对，问题在 **导入后的 Clip 是空的**，或 **Game 窗口 Mute Audio** |
| 播放名是 `gun_pcm16` 且有采样数、仍没声                      | 脚本和 WAV 都通了，查 **编辑器静音 / Windows 默认播放设备**                                         |
| 播放名是 `gun_beep`                                 | 磁盘 WAV 没读到，改用内存生成的短蜂鸣；若蜂鸣也听不见，一定是输出设备或静音，不是文件                                    |


堆栈可能是 `PlayFireSound`（开枪）或 `PlayHitSound`（命中）。两条都走同一个 `PlayClipNow`。看到命中堆栈、弹孔也在，说明播声音的函数已经执行，不要再去查射线。

开火输入约束（容易误判成“没声音”）：

- 必须 **鼠标已锁定**（`Cursor.lockState == Locked`）才会记为 `FirePressed`
- 左键 `Fire1` 或右键均可开火（见 `GameplayInputState.Read()`）
- 未锁定时左键只是重新锁鼠标，不开火

---



## 2. 日志在播、扬声器没声：两条真正的原因

实测过：Console 已有 `[GunShot] 播放音效: gun, 声源: PlayerCamera`，堆栈在 `HitEffectSpawner.PlayClipNow` ← `PlayHitSound`，假人伤情也在跳，**仍然完全没声**。

这时不要再改射击逻辑。优先查下面两件事。

### 2.1 Game 窗口 Mute Audio

Game 视图右上角喇叭图标（Mute Audio）打开时：

- `PlayOneShot` 照样返回，日志照样打
- 编辑器不把声音送到系统扬声器

当前代码会在播放前检测 `EditorUtility.audioMasterMute`，为 true 时自动取消静音，并打：

```
[GunShot] 检测到 Game 窗口 Mute Audio，已自动取消静音。
```

若诊断行里 `editorMute=True` 且没有这句警告，先手动点一下 Game 窗口喇叭，停掉 Play 再进一次。

### 2.2 Unity 导入器把 Clip 弄“空”了

`gun.wav` 磁盘上是有响度的（峰值接近满幅），但进 Unity 后 `PlayOneShot` 仍可能静音。

原始文件曾经是：

- **24 位** PCM（常见音效是 **16 位**）
- **48 kHz 立体声**
- Inspector 里被设成 **Vorbis 压缩**，且 `preloadAudioData = 0`、`3D = 1`

Unity 导入 24 位 WAV 再压成 Vorbis 时，经常出现：

> Clip 能挂上、`PlayOneShot` 也调用了、日志 Clip 名还是 `gun`，扬声器完全没声。

后来即使转成 16 位 PCM，**Inspector 里那支导入资源仍可能是坏的**（Library 缓存没跟上，或导入器仍解出 0 有效波形）。所以不能只信 Inspector 预览和 `PlayOneShot` 的日志。

弹孔是 Mesh/贴图，不走音频管线，所以会“看得见中弹、听不见枪响”。

### 处理办法（本项目当前做法）

1. 磁盘文件保持 **16 位 PCM WAV**（Audacity：导出 → WAV 16-bit PCM）。新音效不要用 24-bit / 32-bit float。
2. Inspector 建议（给以后正规导入用）：


| 项                  | 建议值                | 原因                |
| ------------------ | ------------------ | ----------------- |
| Load Type          | Decompress On Load | 枪声短，一次解压完最稳       |
| Preload Audio Data | 勾选                 | 避免第一次播放时数据还没进内存   |
| Compression Format | PCM                | 短音效别用 Vorbis      |
| 3D Sound           | 不勾选（2D）            | 枪声贴在玩家耳朵上，不要按距离衰减 |


1. **播放时绕过导入器**：`HitEffectSpawner` 用 `File.ReadAllBytes` 读 `Assets/Source/Audio/gun.wav`，自己把 16 位 PCM 解成 `AudioClip.Create("gun_pcm16", ...)` 再 `PlayOneShot`。这支内存 Clip 不经过 Unity AudioImporter。
2. 读文件失败则退回 Inspector 上的 Clip；再不行就播内存生成的 `gun_beep`（880 Hz 短音），用来判断“音频管线死没死”。

成功绕过导入器时应看到 `实际播放=gun_pcm16`，而不是 `gun`。

---



## 3. 场景里没有启用的 AudioListener

本项目同一时刻只允许一个 `AudioListener`（见 `RuntimeUiFactory.SetExclusiveAudioListener`），否则 Unity 会每帧刷警告。


| 时机         | 谁的耳朵是开的                                 |
| ---------- | --------------------------------------- |
| 大厅 / 选阵营   | 俯视摄像机 `_overviewCamera`                 |
| 选完红/蓝、进入操作 | 玩家子物体 `PlayerCamera` 上的 `AudioListener` |


预制体里 `PlayerCamera` 的 Camera 和 AudioListener **默认是关掉的**（`m_Enabled: 0`），要等 `PlayerController.SetViewEnabled(true)` 才会打开。进入第一人称时会在同一物体上预挂 2D `AudioSource`，避免第一枪才 `AddComponent`。

如果进了第一人称、能开枪、但 Listener 仍关着（或被俯视相机抢走），`PlayOneShot` 等于对空气说话。诊断行里 `Listener启用数=0` 就是这种情况。

### 处理办法

开枪时在 **玩家摄像机** 上播，而不是在世界原点的 `HitEffectManager` 上播：

1. 确认 `PlayerCamera` 上有 `AudioListener`，没有就补一个。
2. 若它是禁用的，调用 `RuntimeUiFactory.SetExclusiveAudioListener(listener)`（会关掉其它 Listener，只开这一个）。
3. 在同一物体上拿/加 `AudioSource`：`spatialBlend = 0`（2D）、`mute = false`、`volume = 1`。
4. `PlayOneShot(clip, 1f)`。

对应代码：`PlayerWeapon.PlayMuzzleSound()` → `HitEffectSpawner.PlayFireSound(preferredSource)`。命中音走 `PlayHitSound` → `GetEarAudioSource()`，应落到同一个 `PlayerCamera`。

**2D / 3D 直觉：**

- `spatialBlend = 0`：像耳机直出，跟声源在世界哪一点无关。
- `spatialBlend = 1`：3D 空间音。声源若在 `(0,0,0)`、玩家在远处，会极轻或听不见。

FPS 枪声应贴在摄像机上用 2D；击中声可以以后再改成命中点 3D。

---



## 4. Clip 没挂上，或播的是空 Instance

`HitEffectSpawner` 在场景物体 **HitEffectManager** 上。Inspector 仍建议拖好：

- `defaultDecalPrefab`：弹孔 Quad
- `fleshHitSound` / `helmetPingSound`：击中音（导入资源；当前播放优先用磁盘解码）
- `fireSound`：开枪音（可先与击中音共用同一支 `gun.wav`）

`EnsureInstance()` 在场景漏挂时会 **自动 new 一个空的 HitEffectManager**。空物体没有 Inspector Clip。当前版本仍会尝试读磁盘 `gun.wav`，所以不一定没声，但弹孔 Prefab 会缺。Console 会出现：

```
[HitEffectSpawner] 场景中没有生成器，已自动创建 HitEffectManager。请补弹孔 Prefab 与音效。
```

看到这句：不要用自动创建的那个，回到场景里把真正的 HitEffectManager 补全引用。

另外：`Awake` 里若已有 `Instance`，后醒来的那个会 `Destroy` 自己。要保证 **带引用的那个先成为 Instance**，不要让空的运行时物体抢先。

---



## 5. 推荐播放流程（本项目当前做法）

```
PerformShoot（已确认 FirePressed 且过了 fireRate）
  → PlayMuzzleSound
       → 打开 PlayerCamera 的 AudioListener（独占）
       → 取消编辑器 Mute Audio、AudioListener.pause
       → PlayClipNow：磁盘解码 gun.wav → gun_pcm16；失败则 Inspector Clip；再失败则 gun_beep
  → Hitscan / Projectile
       → 命中后 SpawnHitEffect
            → 弹孔 Quad + 同一套 PlayClipNow（击中音）
```

`PlayerController.SetViewEnabled(true)` 时预先在 `PlayerCamera` 上准备 2D `AudioSource`。

`ResolvePlaybackClip` 只在第一次尝试读磁盘，结果缓存在 `_rawGunClip`。

打包后 `Application.dataPath` 不再指向 `Assets/Source/Audio`，磁盘解码会失败，那时会退回 Inspector Clip。正式发布前要把音效改回正规导入资源，或改放到 `StreamingAssets`。

---



## 6. 以后再遇到“没声音”的检查清单

按顺序勾，不要跳步：

1. Game 窗口有没有 Mute Audio；有没有 `[GunShot] 检测到 ... Mute Audio`。
2. Console 有没有 `[GunShot]`；没有则查开火输入与 `PlayMuzzleSound` 是否执行。
3. 看播放名：`gun` = 仍在用导入资源（可能是空波形）；`gun_pcm16` = 已绕过导入器；`gun_beep` = 文件没读到。
4. 看第一枪的 `[GunShot] 诊断:`：`Listener启用数`、`editorMute`、`samples`、`AudioListener.volume` / `pause`。
5. 磁盘 `gun.wav` 是不是 16-bit PCM；路径是否为 `Assets/Source/Audio/gun.wav`。
6. Play 模式下 Hierarchy 搜 `AudioListener`：是否恰好一个是 Enabled，且挂在当前第一人称相机上。
7. 播声音的 `AudioSource` 是否 Mute、Volume 是否为 0、`spatialBlend` 是否误为 1 且物体在很远的地方。
8. 是否误用了自动创建的空 `HitEffectManager`。
9. 若已是 `gun_pcm16`、Listener=1、`editorMute=False` 仍没声：查 Windows 默认播放设备、系统音量、耳机是否插在别的口上。这时不是游戏脚本问题。

---



## 7. 附：弹孔看得见之前也曾“看不见”

同一套特效里，画面和声音是两条线。弹孔曾消失过，原因与音频无关，避免以后混在一起查：


| 问题            | 原因                                                        | 处理                                     |
| ------------- | --------------------------------------------------------- | -------------------------------------- |
| Quad 完全看不见    | 材质 Standard Cutout 且主贴图为空，被裁成全透明                          | 运行时改用 `Unlit/Color` 深色材质               |
| 贴在墙里 / 背对相机   | Unity 内置 Quad 正面朝本地 **-Z**，`LookRotation(normal)` 会把正面埋进墙 | `LookRotation(-normal)`，沿法线再抬出约 `0.03` |
| Quad 突然变极小或消失 | 父物体是缩放过的 Hitbox（玩家胶囊 `lossyScale` 约 0.5）                  | 生成后 `SetParent(null)`，世界缩放固定为 `0.12`   |


弹孔逻辑在 `HitEffectSpawner.SpawnDecal`。音效通了之后，弹孔仍应保持“不挂父物体、朝向相机一侧”。

---



## 8. 一句话结论

**能出弹孔、甚至 Console 写着** `PlayOneShot` **成功，只说明函数执行了，不说明扬声器有波形。**

本项目实际踩过的静音链：

1. 24 位 WAV + Vorbis 导入 → Inspector 里的 `gun` Clip 播了等于没播
2. 转成 16 位之后日志仍是 `gun` + `PlayerCamera`，照样没声 → 不能信任导入器
3. Game 窗口 Mute Audio 会让“日志成功、耳朵没声”再出现一次

当前可靠播法：**读磁盘 16 位 WAV → 内存** `gun_pcm16` **→ 贴在** `PlayerCamera` **的 2D AudioSource 上播**；同时自动取消编辑器静音。若 `gun_pcm16` 仍完全没声，去查系统播放设备。